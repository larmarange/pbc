<?php
// This script and data application were generated by AppGini 5.92
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/personnes.php");
	include_once("{$currDir}/personnes_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('personnes');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'personnes';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`personnes`.`id`" => "id",
		"`personnes`.`nom`" => "nom",
		"`personnes`.`email`" => "email",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`personnes`.`id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`personnes`.`id`" => "id",
		"`personnes`.`nom`" => "nom",
		"`personnes`.`email`" => "email",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`personnes`.`id`" => "ID",
		"`personnes`.`nom`" => "Pr&#233;nom Nom",
		"`personnes`.`email`" => "Email",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`personnes`.`id`" => "id",
		"`personnes`.`nom`" => "nom",
		"`personnes`.`email`" => "email",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`personnes` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 50;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'personnes_view.php';
	$x->RedirectAfterInsert = 'personnes_view.php';
	$x->TableTitle = 'Individus';
	$x->TableIcon = 'resources/table_icons/023-human-resources.png';
	$x->PrimaryKey = '`personnes`.`id`';
	$x->DefaultSortField = '2';
	$x->DefaultSortDirection = 'asc';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Pr&#233;nom Nom', 'Email', ];
	$x->ColFieldName = ['nom', 'email', ];
	$x->ColNumber  = [2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/personnes_templateTV.html';
	$x->SelectedTemplate = 'templates/personnes_templateTVS.html';
	$x->TemplateDV = 'templates/personnes_templateDV.html';
	$x->TemplateDVP = 'templates/personnes_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `personnes`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='personnes' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `personnes`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='personnes' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`personnes`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: personnes_init
	$render = true;
	if(function_exists('personnes_init')) {
		$args = [];
		$render = personnes_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: personnes_header
	$headerCode = '';
	if(function_exists('personnes_header')) {
		$args = [];
		$headerCode = personnes_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: personnes_footer
	$footerCode = '';
	if(function_exists('personnes_footer')) {
		$args = [];
		$footerCode = personnes_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
